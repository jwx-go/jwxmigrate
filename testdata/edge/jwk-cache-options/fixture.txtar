-- input/main.go --
package example

import (
	"context"
	"time"

	"github.com/lestrrat-go/jwx/v3/jwk"
)

// Realistic jwk cache setup. One fixture covers every rule that fires on
// the Cache/Register subsystem in v3:
//
//   - jwk-cache-removed / jwk-cache-extension — jwk.NewCache(...)
//   - jwk-withconstantinterval-removed         — jwk.WithConstantInterval
//   - jwk-withmininterval-removed              — jwk.WithMinInterval
//   - jwk-withmaxinterval-removed              — jwk.WithMaxInterval
//   - jwk-withhttprcresourceoption-removed     — jwk.WithHttprcResourceOption
//   - jwk-cacheoption-removed                  — jwk.CacheOption (param type)
//   - jwk-resourceoption-removed               — jwk.ResourceOption (param type)
//   - jwk-registeroption-removed               — jwk.RegisterOption (param type)
//   - jwk-registerfetchoption-removed          — jwk.RegisterFetchOption (param type)
//
// The expectation is per-rule: running `jwxmigrate check` surfaces every
// symbol in context, not as isolated stubs.

func makeCache(ctx context.Context, client any, extra []jwk.CacheOption) (any, error) {
	cache, err := jwk.NewCache(ctx, client, extra...)
	if err != nil {
		return nil, err
	}
	return cache, nil
}

func registerURL(ctx context.Context, cache any, url string, resourceOpts []jwk.ResourceOption, regOpts []jwk.RegisterOption, fetchOpts []jwk.RegisterFetchOption) {
	_ = cache
	_ = resourceOpts
	_ = regOpts
	_ = fetchOpts
	_ = url
	_ = ctx
}

func defaultOptions() []jwk.RegisterOption {
	return []jwk.RegisterOption{
		jwk.WithConstantInterval(15 * time.Minute),
		jwk.WithMinInterval(5 * time.Minute),
		jwk.WithMaxInterval(60 * time.Minute),
		jwk.WithHttprcResourceOption(nil),
	}
}
-- want_check.txt --
main.go:7: [import-v3-to-v4] (mechanical) "github.com/lestrrat-go/jwx/v3/jwk"
main.go:26: [jwk-cacheoption-removed] (mechanical) jwk.CacheOption
main.go:27: [jwk-cache-removed] (manual) jwk.NewCache(ctx, client, extra...)
main.go:27: [jwk-cache-extension] (manual) jwk.NewCache(ctx, client, extra...)
main.go:34: [jwk-resourceoption-removed] (mechanical) jwk.ResourceOption
main.go:34: [jwk-registeroption-removed] (mechanical) jwk.RegisterOption
main.go:34: [jwk-registerfetchoption-removed] (mechanical) jwk.RegisterFetchOption
main.go:43: [jwk-registeroption-removed] (mechanical) jwk.RegisterOption
main.go:44: [jwk-registeroption-removed] (mechanical) jwk.RegisterOption
main.go:45: [jwk-withconstantinterval-removed] (mechanical) jwk.WithConstantInterval(15 * time.Minute)
main.go:46: [jwk-withmininterval-removed] (mechanical) jwk.WithMinInterval(5 * time.Minute)
main.go:47: [jwk-withmaxinterval-removed] (mechanical) jwk.WithMaxInterval(60 * time.Minute)
main.go:48: [jwk-withhttprcresourceoption-removed] (mechanical) jwk.WithHttprcResourceOption(nil)

total=13 mechanical=11 manual=2
-- want_fix/main.go --
package example

import (
	"context"
	"time"

	"github.com/lestrrat-go/jwx/v4/jwk"
)

// Realistic jwk cache setup. One fixture covers every rule that fires on
// the Cache/Register subsystem in v3:
//
//   - jwk-cache-removed / jwk-cache-extension — jwk.NewCache(...)
//   - jwk-withconstantinterval-removed         — jwk.WithConstantInterval
//   - jwk-withmininterval-removed              — jwk.WithMinInterval
//   - jwk-withmaxinterval-removed              — jwk.WithMaxInterval
//   - jwk-withhttprcresourceoption-removed     — jwk.WithHttprcResourceOption
//   - jwk-cacheoption-removed                  — jwk.CacheOption (param type)
//   - jwk-resourceoption-removed               — jwk.ResourceOption (param type)
//   - jwk-registeroption-removed               — jwk.RegisterOption (param type)
//   - jwk-registerfetchoption-removed          — jwk.RegisterFetchOption (param type)
//
// The expectation is per-rule: running `jwxmigrate check` surfaces every
// symbol in context, not as isolated stubs.

func makeCache(ctx context.Context, client any, extra []jwk.CacheOption) (any, error) {
	cache, err := jwk.NewCache(ctx, client, extra...)
	if err != nil {
		return nil, err
	}
	return cache, nil
}

func registerURL(ctx context.Context, cache any, url string, resourceOpts []jwk.ResourceOption, regOpts []jwk.RegisterOption, fetchOpts []jwk.RegisterFetchOption) {
	_ = cache
	_ = resourceOpts
	_ = regOpts
	_ = fetchOpts
	_ = url
	_ = ctx
}

func defaultOptions() []jwk.RegisterOption {
	return []jwk.RegisterOption{
		jwk.WithConstantInterval(15 * time.Minute),
		jwk.WithMinInterval(5 * time.Minute),
		jwk.WithMaxInterval(60 * time.Minute),
		jwk.WithHttprcResourceOption(nil),
	}
}
