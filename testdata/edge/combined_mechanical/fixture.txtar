-- input/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v3"
	"github.com/lestrrat-go/jwx/v3/jws"
	"github.com/lestrrat-go/jwx/v3/jwt"
)

// Several mechanical rules in one file. After running fix once:
//   - all v3 imports become v4
//   - Signer2 → Signer
//   - jwt.ReadFile("token.jwt") → jwt.ParseFS(os.DirFS("."), "token.jwt")
//     (adds "os" to the import group)
//   - DecoderSettings → Settings
// A second fix pass must be a no-op and no v3 paths may remain.

type mySigner struct{}

var _ jws.Signer2 = mySigner{}

func (mySigner) Sign(key any, payload []byte) ([]byte, error) { return nil, nil }

func run() {
	tok, _ := jwt.ReadFile("token.jwt")
	_ = tok
	jwx.DecoderSettings(jwx.WithUseNumber(true))
}
-- want_check.txt --
main.go:4: [import-v3-to-v4] (mechanical) "github.com/lestrrat-go/jwx/v3"
main.go:5: [import-v3-to-v4] (mechanical) "github.com/lestrrat-go/jwx/v3/jws"
main.go:6: [import-v3-to-v4] (mechanical) "github.com/lestrrat-go/jwx/v3/jwt"
main.go:19: [jws-signer2-to-signer] (mechanical) jws.Signer2
main.go:24: [readfile-to-parsefs] (mechanical) jwt.ReadFile("token.jwt")
main.go:26: [rename-decodersettings-to-settings] (mechanical) DecoderSettings

total=6 mechanical=6 manual=0
-- want_fix/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v4"
	"github.com/lestrrat-go/jwx/v4/jws"
	"github.com/lestrrat-go/jwx/v4/jwt"
	"os"
)

// Several mechanical rules in one file. After running fix once:
//   - all v3 imports become v4
//   - Signer2 → Signer
//   - jwt.ReadFile("token.jwt") → jwt.ParseFS(os.DirFS("."), "token.jwt")
//     (adds "os" to the import group)
//   - DecoderSettings → Settings
// A second fix pass must be a no-op and no v3 paths may remain.

type mySigner struct{}

var _ jws.Signer = mySigner{}

func (mySigner) Sign(key any, payload []byte) ([]byte, error) { return nil, nil }

func run() {
	tok, _ := jwt.ParseFS(os.DirFS("."), "token.jwt")
	_ = tok
	jwx.Settings(jwx.WithUseNumber(true))
}
