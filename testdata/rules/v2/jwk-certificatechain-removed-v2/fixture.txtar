-- input/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v2/jwk"
)

// jwk.CertificateChain was removed in v2 in favor of *cert.Chain.
// Surviving v2 code that still references it needs migration.

var _ jwk.CertificateChain
-- want_check.txt --
main.go:10: [jwk-certificatechain-removed-v2] (manual) jwk.CertificateChain

total=1 mechanical=0 manual=1
-- want_fix/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v4/jwk"
)

// jwk.CertificateChain was removed in v2 in favor of *cert.Chain.
// Surviving v2 code that still references it needs migration.

var _ jwk.CertificateChain
