-- input/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v2/jws"
)

func verify(payload []byte, key any) ([]byte, error) {
	return jws.Verify(payload, jws.WithVerify(nil, key))
}
-- want_check.txt --
main.go:8: [jws-withverify-removed-v2] (manual) jws.WithVerify(nil, key)

total=1 mechanical=0 manual=1
-- want_fix/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v4/jws"
)

func verify(payload []byte, key any) ([]byte, error) {
	return jws.Verify(payload, jws.WithVerify(nil, key))
}
