-- input/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v2/jwt"
)

func load(path string, opts ...jwt.ReadFileOption) error {
	_, err := jwt.ReadFile(path, opts...)
	return err
}
-- want_check.txt --
main.go:7: [remove-readfileoption-v2] (mechanical) ReadFileOption

total=1 mechanical=1 manual=0
-- want_fix/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v4/jwt"
)

func load(path string, opts ...jwt.ParseOption) error {
	_, err := jwt.ReadFile(path, opts...)
	return err
}
