-- input/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v3/jwt"
)

func init() {
	jwt.Settings(jwt.WithMaxParseInputSize(1 << 20))
}
-- want_check.txt --
main.go:8: [settings-returns-error] (manual) jwt.Settings(jwt.WithMaxParseInputSize(1 << 20))

total=1 mechanical=0 manual=1
-- want_fix/main.go --
package example

import (
	"github.com/lestrrat-go/jwx/v4/jwt"
)

func init() {
	jwt.Settings(jwt.WithMaxParseInputSize(1 << 20))
}
